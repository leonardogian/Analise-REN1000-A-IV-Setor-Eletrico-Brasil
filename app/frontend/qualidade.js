/**
 * qualidade.js — Data Quality & Provenance page
 * Static content — minimal JS for page initialization
 */
(function () {
    'use strict';
    // Page is fully static HTML — no data fetch needed.
    // This script exists to follow the module pattern convention.
})();
